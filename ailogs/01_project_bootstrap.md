# 01 - Project Bootstrap (Vite + P0)

## Primary Prompt A
Read all documents in `/PRDs` (especially `RPD.md`, `UI.md`, `development.md`), then create a 0-to-1 development plan.  
Constraints:
- Final stack: **Vite**
- Demo deployment: **GitHub Pages public URL**
- Base map and POI initial data: **mock first**
- Check-in: **button + mock result first**
- This round only targets **P0**

## Primary Prompt B
Split the plan into an executable task board by module, then start implementation.

## Primary Prompt C
Guide deployment to a new GitHub repository and verify GitHub Pages availability.
